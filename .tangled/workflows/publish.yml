when:
  - event:
      - push
    branch:
      - main
  - event:
      - manual

engine: nixery

dependencies:
  nixpkgs:
    - curl

environment:
  SITE_PATH: zig-out/docs
  SITE_NAME: notmuch.zig
  TERM: dumb
  NO_COLOR: "true"

steps:
  - name: Build site
    command: nix develop -c zig build docs

  - name: deploy to wisp
    command: |
      curl -s -o ./wisp-cli https://sites.wisp.place/nekomimi.pet/wisp-cli-binaries/wisp-cli-x86_64-linux
      chmod a+x ./wisp-cli
      ./wisp-cli \
        "$WISP_HANDLE" \
        --path "$SITE_PATH" \
        --name "$SITE_NAME" \
        --password "$WISP_APP_PASSWORD"
